# Copy this file to .env and set your Discord bot token
DISCORD_TOKEN=your_discord_token_here

# Optional
# Where the SQLite DB file is stored (use a mounted volume in Docker)
DB_FILE=./voice_data.db

# Optional
# If true, creates a timestamped backup file next to DB_FILE on startup (copy, not move)
DB_BACKUP_ON_START=false
